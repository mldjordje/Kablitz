# Richard Kablitz GmbH — Demo

Standalone Next.js export of the Kablitz concept demo (originally `/demo/kablitz-gmbh-r4t9k2` in `bad-mergentheim-demo-hub`).

- `/` — landing page
- `/admin` — admin preview

```bash
npm install
npm run dev
```
